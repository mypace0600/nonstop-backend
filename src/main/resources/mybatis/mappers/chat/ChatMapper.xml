<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.nonstop.mapper.ChatMapper">

    <insert id="insertMessage" parameterType="com.app.nonstop.domain.chat.dto.ChatMessageDto"
            useGeneratedKeys="true" keyProperty="messageId">
        INSERT INTO messages (chat_room_id, sender_id, content, type, client_message_id, sent_at)
        VALUES (#{roomId}, #{senderId}, #{content}, #{type}::message_type, #{clientMessageId}, #{sentAt})
    </insert>

</mapper>
