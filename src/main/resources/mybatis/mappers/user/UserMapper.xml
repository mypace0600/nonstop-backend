<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.app.nonstop.mapper.UserMapper">

    <select id="findById" resultType="com.app.nonstop.domain.user.entity.User">
        SELECT * FROM users WHERE id = #{id} AND deleted_at IS NULL
    </select>

    <update id="updateUniversity">
        UPDATE users
        SET university_id = #{universityId},
            major_id = #{majorId},
            updated_at = now()
        WHERE id = #{userId}
          AND deleted_at IS NULL
    </update>

    <update id="updateVerificationStatus">
        UPDATE users
        SET is_verified = #{isVerified},
            verification_method = #{verificationMethod}::verification_method,
            updated_at = now()
        WHERE id = #{userId}
          AND deleted_at IS NULL
    </update>

    <select id="searchByNickname" resultType="com.app.nonstop.domain.user.dto.UserResponseDto">
        SELECT u.id, u.id AS user_id, u.email, u.nickname, u.university_id, u.major_id, u.profile_image_url, u.introduction, u.preferred_language, u.is_verified
        FROM users u
        WHERE u.nickname LIKE CONCAT('%', #{query}, '%')
          AND u.deleted_at IS NULL
        LIMIT 20
    </select>

    <update id="updateProfileImage">
        UPDATE users
        SET profile_image_url = #{profileImageUrl},
            updated_at = now()
        WHERE id = #{userId}
          AND deleted_at IS NULL
    </update>

</mapper>
